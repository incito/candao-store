<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.candao.www.dataserver.mapper.OrderMemberMapper">
    <resultMap id="baseMap" type="com.candao.www.dataserver.entity.OrderMember">
        <result column="valid" property="valid" jdbcType="VARCHAR"/>
        <result column="serial" property="serial" jdbcType="VARCHAR"/>
        <result column="cardNo" property="cardno" jdbcType="VARCHAR"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.candao.www.dataserver.entity.OrderMember">
        INSERT INTO t_order_member (cardno,orderid, userid, ordertime, operatetype, business, terminal, serial, batchno, businessname, score,coupons, stored, scorebalance, couponsbalance, storedbalance,psexpansivity,netvalue,Inflated)
        VALUES (#{cardno},#{orderid},#{userid},#{ordertime},#{operatetype},#{business}, #{terminal}, #{serial}, #{batchno}, #{businessname}, #{score},#{coupons}, #{stored}, #{scorebalance}, #{couponsbalance}, #{storedbalance},#{psexpansivity},#{netvalue},#{Inflated})
    </insert>
    <delete id="deleteByOrderId" parameterType="String">
      delete from t_order_member where orderid=#{orderId}
    </delete>
    <select id="selectByOrderId" parameterType="String" resultMap="baseMap">
      select valid,serial,cardno from t_order_member where orderid=#{orderId} limit 1
    </select>
    <update id="updateValid" parameterType="com.candao.www.dataserver.entity.OrderMember">
        update t_order_member set valid=1 where orderid=#{orderid}
    </update>
    <select id="selectValid" parameterType="Map" resultType="String">
        select valid from  t_order_member where orderid=#{orderId} and valid=0  LIMIT 1
    </select>
</mapper>
